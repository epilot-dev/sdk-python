"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from datetime import datetime
from epilot_customer_portal import models, utils
from epilot_customer_portal._hooks import HookContext
from epilot_customer_portal.types import OptionalNullable, UNSET
from typing import Mapping, Optional


class Activity(BaseSDK):
    def get_entity_activity_feed(
        self,
        *,
        id: str,
        slug: models.EntitySlug,
        after: Optional[datetime] = None,
        before: Optional[datetime] = None,
        from_: Optional[int] = 0,
        include_relations: Optional[bool] = False,
        size: Optional[int] = 25,
        type_: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.GetEntityActivityFeedResponseBody:
        r"""getEntityActivityFeed

        Get activity feed for an entity


        :param id: Entity id
        :param slug: Entity Type
        :param after: Get activities after this timestamp
        :param before: get activities before this timestamp
        :param from_: start from page
        :param include_relations: Include activities from related entities
        :param size: max number of results to return
        :param type: Filter by activity type
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url

        request = models.GetEntityActivityFeedRequest(
            after=after,
            before=before,
            from_=from_,
            id=id,
            include_relations=include_relations,
            size=size,
            slug=slug,
            type=type_,
        )

        req = self.build_request(
            method="GET",
            path="/v2/portal/entity/{slug}/{id}/activity",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                operation_id="getEntityActivityFeed",
                oauth2_scopes=[],
                security_source=self.sdk_configuration.security,
            ),
            request=req,
            error_status_codes=["4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return utils.unmarshal_json(
                http_res.text, models.GetEntityActivityFeedResponseBody
            )
        if utils.match_response(http_res, ["4XX", "5XX"], "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError(
                "API error occurred", http_res.status_code, http_res_text, http_res
            )

        content_type = http_res.headers.get("Content-Type")
        http_res_text = utils.stream_to_text(http_res)
        raise models.APIError(
            f"Unexpected response received (code: {http_res.status_code}, type: {content_type})",
            http_res.status_code,
            http_res_text,
            http_res,
        )

    async def get_entity_activity_feed_async(
        self,
        *,
        id: str,
        slug: models.EntitySlug,
        after: Optional[datetime] = None,
        before: Optional[datetime] = None,
        from_: Optional[int] = 0,
        include_relations: Optional[bool] = False,
        size: Optional[int] = 25,
        type_: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.GetEntityActivityFeedResponseBody:
        r"""getEntityActivityFeed

        Get activity feed for an entity


        :param id: Entity id
        :param slug: Entity Type
        :param after: Get activities after this timestamp
        :param before: get activities before this timestamp
        :param from_: start from page
        :param include_relations: Include activities from related entities
        :param size: max number of results to return
        :param type: Filter by activity type
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url

        request = models.GetEntityActivityFeedRequest(
            after=after,
            before=before,
            from_=from_,
            id=id,
            include_relations=include_relations,
            size=size,
            slug=slug,
            type=type_,
        )

        req = self.build_request_async(
            method="GET",
            path="/v2/portal/entity/{slug}/{id}/activity",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                operation_id="getEntityActivityFeed",
                oauth2_scopes=[],
                security_source=self.sdk_configuration.security,
            ),
            request=req,
            error_status_codes=["4XX", "5XX"],
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return utils.unmarshal_json(
                http_res.text, models.GetEntityActivityFeedResponseBody
            )
        if utils.match_response(http_res, ["4XX", "5XX"], "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError(
                "API error occurred", http_res.status_code, http_res_text, http_res
            )

        content_type = http_res.headers.get("Content-Type")
        http_res_text = await utils.stream_to_text_async(http_res)
        raise models.APIError(
            f"Unexpected response received (code: {http_res.status_code}, type: {content_type})",
            http_res.status_code,
            http_res_text,
            http_res,
        )
