"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .notificationitem import NotificationItem, NotificationItemTypedDict
from epilot_notification.types import BaseModel
from epilot_notification.utils import FieldMetadata, QueryParamMetadata
from typing import List, Optional, TypedDict
from typing_extensions import Annotated, NotRequired


class GetNotificationsRequestTypedDict(TypedDict):
    after_id: NotRequired[int]
    limit: NotRequired[int]
    r"""The numbers of items to return"""
    no_hydrate: NotRequired[bool]
    r"""When true, the payload will not be hydrated with the entity data. This is useful when the client does not need the entity data and wants to save on API calls (performance gain). When false, the payload will be hydrated with the entity data. This is useful when the client needs the entity data to display the notification (e.g. to show the name of the contact in the notification message), but can have a significative performance impact.

    This endpoint will eventually be deprecated in favor of GET /v2/notification/notifications which no longer hydrates the payload by default.

    """
    

class GetNotificationsRequest(BaseModel):
    after_id: Annotated[Optional[int], FieldMetadata(query=QueryParamMetadata(style="form", explode=True))] = None
    limit: Annotated[Optional[int], FieldMetadata(query=QueryParamMetadata(style="form", explode=True))] = None
    r"""The numbers of items to return"""
    no_hydrate: Annotated[Optional[bool], FieldMetadata(query=QueryParamMetadata(style="form", explode=True))] = None
    r"""When true, the payload will not be hydrated with the entity data. This is useful when the client does not need the entity data and wants to save on API calls (performance gain). When false, the payload will be hydrated with the entity data. This is useful when the client needs the entity data to display the notification (e.g. to show the name of the contact in the notification message), but can have a significative performance impact.

    This endpoint will eventually be deprecated in favor of GET /v2/notification/notifications which no longer hydrates the payload by default.

    """
    

class GetNotificationsResponseBodyTypedDict(TypedDict):
    r"""Success"""
    
    results: NotRequired[List[NotificationItemTypedDict]]
    total: NotRequired[int]
    total_unread: NotRequired[int]
    

class GetNotificationsResponseBody(BaseModel):
    r"""Success"""
    
    results: Optional[List[NotificationItem]] = None
    total: Optional[int] = None
    total_unread: Optional[int] = None
    
