"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from .httpclient import AsyncHttpClient, HttpClient
from .sdkconfiguration import SDKConfiguration
from .utils.logger import Logger, NoOpLogger
from .utils.retries import RetryConfig
from epilot_entity import models, utils
from epilot_entity._hooks import SDKHooks
from epilot_entity.activity_sdk import ActivitySDK
from epilot_entity.entities import Entities
from epilot_entity.entity_import import EntityImport
from epilot_entity.export import Export
from epilot_entity.relations import Relations
from epilot_entity.saved_views import SavedViews
from epilot_entity.schemas import Schemas
from epilot_entity.taxonomy_sdk import TaxonomySDK
from epilot_entity.types import OptionalNullable, UNSET
import httpx
from typing import Callable, Dict, Optional, Union

class Epilot(BaseSDK):
    r"""Entity API: Flexible data layer for epilot Entities.

    Use this API configure and access your business objects like Contacts, Opportunities and Products.

    [Feature Documentation](https://docs.epilot.io/docs/entities/flexible-entities)

    """
    activity: ActivitySDK
    r"""Entity Events"""
    schemas: Schemas
    r"""Model Entities"""
    taxonomy: TaxonomySDK
    r"""Taxonomies and Classifications"""
    saved_views: SavedViews
    r"""Saved Views for Entities"""
    entities: Entities
    r"""CRUD Access for Entities"""
    relations: Relations
    r"""Entity Relationships"""
    export: Export
    r"""Export and Import entities via files"""
    entity_import: EntityImport
    def __init__(
        self,
        security: Union[models.Security, Callable[[], models.Security]],
        server_idx: Optional[int] = None,
        server_url: Optional[str] = None,
        url_params: Optional[Dict[str, str]] = None,
        client: Optional[HttpClient] = None,
        async_client: Optional[AsyncHttpClient] = None,
        retry_config: OptionalNullable[RetryConfig] = UNSET,
        timeout_ms: Optional[int] = None,
        debug_logger: Optional[Logger] = None
    ) -> None:
        r"""Instantiates the SDK configuring it with the provided parameters.

        :param security: The security details required for authentication
        :param server_idx: The index of the server to use for all methods
        :param server_url: The server URL to use for all methods
        :param url_params: Parameters to optionally template the server URL with
        :param client: The HTTP client to use for all synchronous methods
        :param async_client: The Async HTTP client to use for all asynchronous methods
        :param retry_config: The retry configuration to use for all supported methods
        :param timeout_ms: Optional request timeout applied to each operation in milliseconds
        """
        if client is None:
            client = httpx.Client()

        assert issubclass(
            type(client), HttpClient
        ), "The provided client must implement the HttpClient protocol."

        if async_client is None:
            async_client = httpx.AsyncClient()

        if debug_logger is None:
            debug_logger = NoOpLogger()

        assert issubclass(
            type(async_client), AsyncHttpClient
        ), "The provided async_client must implement the AsyncHttpClient protocol."

        if server_url is not None:
            if url_params is not None:
                server_url = utils.template_url(server_url, url_params)
    

        BaseSDK.__init__(self, SDKConfiguration(
            client=client,
            async_client=async_client,
            security=security,
            server_url=server_url,
            server_idx=server_idx,
            retry_config=retry_config,
            timeout_ms=timeout_ms,
            debug_logger=debug_logger
        ))

        hooks = SDKHooks()

        current_server_url, *_ = self.sdk_configuration.get_server_details()
        server_url, self.sdk_configuration.client = hooks.sdk_init(current_server_url, self.sdk_configuration.client)
        if current_server_url != server_url:
            self.sdk_configuration.server_url = server_url

        # pylint: disable=protected-access
        self.sdk_configuration.__dict__["_hooks"] = hooks

        self._init_sdks()


    def _init_sdks(self):
        self.activity = ActivitySDK(self.sdk_configuration)
        self.schemas = Schemas(self.sdk_configuration)
        self.taxonomy = TaxonomySDK(self.sdk_configuration)
        self.saved_views = SavedViews(self.sdk_configuration)
        self.entities = Entities(self.sdk_configuration)
        self.relations = Relations(self.sdk_configuration)
        self.export = Export(self.sdk_configuration)
        self.entity_import = EntityImport(self.sdk_configuration)
    
